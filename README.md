# Excel Data Processor & Consolidator

Follow these commands to run the application:

```bash
# 1. Install dependencies
pip install -r requirements.txt

# 2. Run the Streamlit application
python -m streamlit run app.py
```
